<%= form_for(@quiz) do |f| %>
  <% if @quiz.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quiz.errors.count, "error") %> prohibited this quiz from being saved:</h2>

      <ul>
      <% @quiz.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Animal1 %><br>
    <!--<%= f.text_field :animal_id %>
    <!-- <%= f.select(:animal, Animal.all.map{|p| [p.avatar.url(:thumb)]}) %> -->


    <table>


  <tbody>
    <!--<% @animals = Animal.all %>
    <% @animals.each do |animal| %>
      <tr>
        <td><%= f.radio_button(:animal_id, animal.id) %></td>
        <!--<td><%= animal.name %></td>
        <td class="image-zoom"><%= image_tag animal.avatar.url(:thumb) %></td>
      </tr>
    <% end %>-->

   <% @animals = Animal.all %>
    <% @animals.each do |animal| %>
      <span class="img-radio">
        <%= f.radio_button(:animal_id, animal.id) %>

       <%= image_tag animal.avatar.url(:thumb) %>
      </span>
    <% end %>
  </tbody>
</table>


  </div>
  <div class="field">
    <%= f.label :Animal2 %><br>
    <!--<%= f.text_field :alternative_id %> -->
<table>


  <tbody>

    <!--<% @animals.each do |alternative| %>
      <tr>
        <td><%= f.radio_button(:alternative_id, alternative.id) %></td>
        <!--td><%= alternative.name %></td>
        <td class="image-zoom"><%= image_tag alternative.avatar.url(:thumb) %></td>
      </tr>
    <% end %>-->

<% @animals = Animal.all %>
    <% @animals.each do |alternative| %>
      <span class="img-radio">
        <%= f.radio_button(:alternative_id, alternative.id) %>

       <%= image_tag alternative.avatar.url(:thumb) %>
      </span>
<% end %>
  </tbody>
</table>

  </div>
  <div class="field">
    <%= f.label :difficulty %><br>
    <%= f.radio_button(:difficulty, "easy") %>
    <%= f.label(:difficulty, "easy") %>
    <%= f.radio_button(:difficulty, "medium") %>
    <%= f.label(:difficulty, "medium") %>
    <%= f.radio_button(:difficulty, "hard") %>
    <%= f.label(:difficulty, "hard") %>


  </div>



  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

